openai-whisper
# whisper는 시스템에 ffmpeg가 설치되어 있어야 합니다 (예: apt install ffmpeg / brew install ffmpeg).

# google 엔진(온라인, 비공식 무료 API)을 사용하려면 아래 주석을 해제하세요.
# 최근 Google의 비공식 엔드포인트가 자주 'Bad Request'로 실패하니 기본값인 whisper를 권장합니다.
# SpeechRecognition>=3.10.0
